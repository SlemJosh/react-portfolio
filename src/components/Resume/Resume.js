import React from 'react';

const Resume = () => {
    return (
        <section>
            <h2>Resume</h2>
            {/* Add resume link and proficiency list here */}
        </section>
    );
};

export default Resume;