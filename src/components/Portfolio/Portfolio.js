import React from 'react';

const Portfolio = () => {
    return (
        <section>
            <h2>Portfolio</h2>
            {/* Add project details here */}
        </section>
    );
};

export default Portfolio;
